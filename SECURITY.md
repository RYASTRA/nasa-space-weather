# Security Policy

## Reporting a Vulnerability

Please open a private security advisory via the repository's **Security** tab.
Do not open a public issue for security reports.

## Scope

This project holds no user data. Its only secret is `NASA_API_KEY`, supplied as a
GitHub Actions secret and never committed. `.env` is gitignored.
